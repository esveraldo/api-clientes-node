services:

  api-clientes:

    build:
      context: .
      dockerfile: Dockerfile

    container_name: api-clientes-node

    ports:
      - "8080:8080"

    environment:
      PORT: 8080

    restart: unless-stopped