services:

  api-clientes:

    build:
      context: .
      dockerfile: Dockerfile

    container_name: api-clientes-node

    ports:
      - "80:8080"

    environment:
      PORT: 8080

    restart: unless-stopped